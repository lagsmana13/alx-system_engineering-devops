#!/usr/bin/env bash
# Script to display the PID, along with the process name, of processes containing the word "bash"
# Does not use the ps command

# Use the pgrep command to find the process IDs and names of processes matching the given criteria
# -l: List the process name along with the PID
# bash: Matches processes whose name contains the word "bash"
pgrep -l bash
