#!/usr/bin/env bash
# Script to display a list of currently running processes
# Shows all processes, for all users, including those without a TTY
# Outputs in a user-oriented format
# Displays process hierarchy

# Use the ps command to retrieve information about running processes
# -a: Display processes for all users
# -x: Include processes without a controlling terminal
# -u: Display additional information about the processes
# -f: Full-format listing, providing a user-oriented format
ps -a -x -u -f
